<?xml version="1.0" encoding="utf-8"?>

<!-- menu -->
<menu
        xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- tasks -->
    <item
            android:id="@+id/navigation_tasks"
            android:icon="@drawable/ic_tasks_black_24dp"
            android:title="@string/tasks"/>

    <!-- journal -->
    <item
            android:id="@+id/navigation_journal"
            android:icon="@drawable/ic_journal_black_24dp"
            android:title="@string/journal"/>

    <!-- profile -->
    <item
            android:id="@+id/navigation_profile"
            android:icon="@drawable/ic_profile_black_24dp"
            android:title="@string/profile"/>

</menu>
